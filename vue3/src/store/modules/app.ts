const state = {
  loading: false
}
const getters = {

}

const mutations = {
  SET_LOADING(state: any, data: any) {
    state.loading = data
  }
}

const actions = {

}

export default {
  namespaced: true,
  state,
  mutations,
  actions,
  getters
}
